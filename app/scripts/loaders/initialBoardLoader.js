class InitialBoardLoader extends Loader {
  constructor() {
    super();
    this.SUFFIX = ".board.gbb";
  }

  getFile(context) {
    // TODO
  }

  readContent(context, content, fileName) {
    // TODO
  }
}
